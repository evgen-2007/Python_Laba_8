# -*- coding: utf-8 -*-

# Визначаємо ім'я файлу, з яким будемо працювати
FILE_NAME = "task_exchange.txt"
STUDENT_LASTNAME = "Мамонтов"

# Питання для наступного студента
QUESTION_TEXT = (
    "Чим відрізняються `list` (список) та `tuple` (кортеж) у Python? \n"
    "Опишіть їхні основні властивості (змінність/незмінність), \n"
    "синтаксис та наведіть приклади, коли доцільно використовувати \n"
    "кожен із цих типів даних."
)

def create_initial_file(filename, lastname, question):
    """
    Створює початковий файл, записує прізвище та питання.
    Використовує режим 'w' (write) для створення нового файлу 
    або перезапису існуючого.
    """
    print(f"Спроба створити та записати дані у файл: {filename}")
    try:
        # 'with open(...)' - це менеджер контексту, який гарантує,
        # що файл буде автоматично закрито, навіть якщо виникне помилка.
        # 'encoding='utf-8'' - важливо для коректного запису кирилиці.
        with open(filename, 'w', encoding='utf-8') as f:
            
            # Записуємо блок інформації першого студента
            f.write("--- Студент 1 ---\n")
            f.write(f"Прізвище: {lastname}\n")
            f.write("Питання:\n")
            f.write(f"{question}\n")
            f.write("-------------------\n\n") # Розділювач для наступного студента
            
        print(f"Файл '{filename}' успішно створено та записано.")

    except PermissionError:
        # Ця помилка виникає, якщо у програми немає прав
        # на запис у вказану директорію.
        print(f"ПОМИЛКА: Недостатньо прав для запису у файл '{filename}'.")
        print("Перевірте права доступу або запустіть скрипт з правами адміністратора.")
        
    except IOError as e:
        # Загальна помилка вводу/виводу (наприклад, якщо диск переповнений)
        print(f"ПОМИЛKA вводу/виводу: {e}")
        
    except Exception as e:
        # Обробка будь-яких інших неочікуваних помилок
        print(f"Виникла неочікувана помилка: {e}")

# --- Запуск функції ---
if __name__ == "__main__":
    create_initial_file(FILE_NAME, STUDENT_LASTNAME, QUESTION_TEXT)

# -*- coding: utf-8 -*-

# --- Дані Студента 2 Ткаченко ---

# Ім'я файлу, який створив перший студент
FILE_NAME = "task_exchange.txt" 
STUDENT_LASTNAME = "Ткаченко"

# Ваша відповідь на питання Студента 1
ANSWER_TEXT = (
    "Відповідь:\n"
    "У Python список (list) - це змінна послідовність елементів, що створюється за допомогою квадратних дужок [] і дозволяє додавати, видаляти чи змінювати значення.\n"
    "Списки доцільно використовувати тоді, коли потрібно змінювати вміст.\n"
    "tuple) — незмінна послідовність, яка створюється за допомогою круглих дужок (), і після створення її вміст не можна змінити.\n"
    "Кортежі доцільно використовувати для збереження фіксованих наборів даних, наприклад координат або констант."
)

# Ваше питання для наступного (третього) студента
QUESTION_TEXT = (
    "Що таке словник (dict) у Python? Опишіть його структуру, "
    "та наведіть приклади використання."
)

def append_to_file(filename, lastname, answer, question):
    print(f"Спроба доповнити файл: {filename}")
    try:
        # 'a' - (append) - це ключ. Цей режим додає текст у кінець файлу, 
        # не стираючи те, що було до цього.
        with open(filename, 'a', encoding='utf-8') as f:
            
            # Записуємо блок інформації другого студента
            f.write("--- Студент 2 ---\n")
            f.write(f"Прізвище: {lastname}\n")
            f.write(f"{answer}\n\n")  # Додаємо \n\n для відступу
            f.write("Питання:\n")
            f.write(f"{question}\n")
            f.write("-------------------\n\n") # Розділювач для Студента 3
            
        print(f"Дані студента '{lastname}' успішно додано у файл '{filename}'.")

    except FileNotFoundError:
        # Ця помилка виникне, якщо файл task_exchange.txt не знайдено
        print(f"ПОМИЛКА: файл '{filename}' не знайдено. "
              "Переконайтеся, що він лежить у тій самій папці, що і скрипт.")
    
    except PermissionError:
        # Ця помилка виникає, якщо у програми немає прав
        # на запис у цей файл (наприклад, він "тільки для читання").
        print(f"ПОМИЛКА: Недостатньо прав для запису у файл '{filename}'.")

    except IOError as e:
        # Загальна помилка вводу/виводу
        print(f"ПОМИЛKA вводу/виводу: {e}")

    except Exception as e:
        # Обробка будь-яких інших неочікуваних помилок
        print(f"Виникла неочікувана помилка: {e}")

# --- Запуск функції ---
# Цей блок коду виконається, коли ви запустите цей файл
if __name__ == "__main__":
    append_to_file(FILE_NAME, STUDENT_LASTNAME, ANSWER_TEXT, QUESTION_TEXT)

# -*- coding: utf-8 -*-

# --- Дані Студента 3 (Житник Ксенія) ---

# Ім'я спільного файлу, з яким працює команда
FILE_NAME = "task_exchange.txt"
STUDENT_LASTNAME = "Житник"

# Відповідь на питання Студента 2
# ("Що таке словник (dict) у Python? Опишіть його структуру, 
# та наведіть приклади використання.")
ANSWER_TEXT = (
    "Відповідь:\n"
    "Словник (dict) у Python - це вбудована структура даних, яка зберігає елементи у форматі 'ключ:значення'.\n"
    "Структура: Словники створюються за допомогою фігурних дужок {}. \n"
    "Кожен елемент - це пара, де 'ключ' (унікальний та незмінний тип, напр. рядок) використовується для доступу до 'значення' (може бути будь-якого типу).\n"
    "Властивості: Словники є змінними (можна додавати/видаляти елементи) та, починаючи з Python 3.7, є впорядкованими (зберігають порядок вставки).\n"
    "Приклад використання (збереження даних профілю):\n"
    "user_profile = {\n"
    "    'username': 'kseniia_zhytnyk',\n"
    "    'role': 'student',\n"
    "    'id': 3\n"
    "}\n"
    "# Доступ до значення за ключем:\n"
    "# print(user_profile['role']) # Виведе 'student'"
)

# Питання для Студента 4
QUESTION_TEXT = (
    "Яке призначення мають блоки `try`, `except` та `finally` в Python?\n"
    "Опишіть, як вони працюють разом для обробки виняткових ситуацій,\n"
    "та поясніть, у якому випадку використання блоку `finally` є особливо корисним."
)

def append_to_file(filename, lastname, answer, question):
    print(f"Спроба доповнити файл: {filename} (Студент: {lastname})")
    try:
        # Використання 'a' (append) додає дані в кінець, не стираючи вміст.
        # encoding='utf-8' є обов'язковим для коректної роботи з кирилицею.
        with open(filename, 'a', encoding='utf-8') as f:
            
            # Запис блоку інформації Студента 3
            f.write("--- Студент 3 ---\n") 
            f.write(f"Прізвище: {lastname}\n")
            f.write(f"{answer}\n\n")
            f.write("Питання:\n")
            f.write(f"{question}\n")
            f.write("-------------------\n\n") # Розділювач для Студента 4
            
        print(f"Дані студента '{lastname}' успішно додано у файл.")

    except FileNotFoundError:
        # Обробка помилки, якщо файл 'task_exchange.txt' не знайдено.
        print(f"ПОМИЛКА: файл '{filename}' не знайдено. "
              "Переконайтеся, що Студент 1 і 2 вже запустили свій код.")
        
    except PermissionError:
        # Обробка помилки, якщо немає прав на запис у файл.
        print(f"ПОМИЛКА: Недостатньо прав для запису у файл '{filename}'.")
        
    except Exception as e:
        # Обробка будь-яких інших неочікуваних помилок.
        print(f"Виникла неочікувана помилка: {e}")

# --- Запуск функції ---
if __name__ == "__main__":
    append_to_file(FILE_NAME, STUDENT_LASTNAME, ANSWER_TEXT, QUESTION_TEXT)



